!!! fix refreshing token
password&login validation
prevent id incrementation
add the ability to delete a token (logout on server side)
make fakes for auth work if env var set (like 'AUTH_USE_FAKE=1', 'AUTH_FAKE_ID=16' or something like this)
add the ability to update teacher account info